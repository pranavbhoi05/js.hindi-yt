const accountId="12345"
let accountEmail="pb@pb.com"
var accountPassword="212121"
accountCity="pune"

//accountId="2"// not allow to use cause constant dont change

accountEmail="meme.com"
accountPassword="69"
accountCity="mumbai"


console.log(accountId);

// never use var keyword. we use let insted cause var can cause trouble(let is similar as var)//

//we wants to declare multiple variable so that we use table so we can add all variable in same line//
 
console.table([accountId;accountEmail;accountPassword;accountCity])

